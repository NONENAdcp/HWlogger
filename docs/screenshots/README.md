# Screenshot placeholders

Replace this file with release screenshots named:

- `sensors.png`
- `graphs.png`
- `logs.png`

The root README already references these paths so the images will appear
without documentation changes once captured.
