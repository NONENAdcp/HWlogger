# Contributing

Use Python 3.11+, create `.venv`, install `requirements-dev.txt`, and run
`ruff check .` and `pytest` before submitting changes. Keep all sensor access
read-only and never add machine-specific paths or personal data.
